# 1️⃣ Classificador de nível de Herói (Desafio 1)

Arquivos: 
-desafio.js;
-desafio.md; 

Script em JavaScript que lê o `nome` e o `XP` do herói pelo terminal e imprime o nível correspondente.

Como executar:

1. Abra um terminal no diretório do projeto (ex: `d:\Projetos\Desafio-DIO-Classificador-nivel-Her-i`).
2. Execute:

```powershell
node desafio.js
```

Exemplo de uso (entrada e saída esperada):

- Entrada:
  - `Nome do herói: Arthur`
  - `XP do herói: 6500`
- Saída:
  - `O Herói de nome Arthur está no nível de Diamante`

Observações:
- O script aceita números com `.` ou `,` como separador de milhares (ex: `6.500` ou `6,500`).

# 2️⃣ Calculadora de partidas Rankeadas (Desafio 2)

Arquivos:
-desafio2.js;
-desafio2.md;

Como executar:

1. Abra um terminal no diretório do projeto (ex: `d:\Projetos\Desafio-DIO-Classificador-nivel-Her-i`).
2. Execute:

```powershell
node desafio2.js
```

Exemplo de uso (saída esperada):

- Saída:
  -`O Herói tem de saldo de 107 vitórias, e está no nível Imortal 🔥!`

# 3️⃣ Escrevendo as classes de um Jogo (Desafio 3)

Arquivos:
-desafio3.js;
-desafio3.md;

Como executar:

1. Abra um terminal no diretório do projeto (ex: `d:\Projetos\Desafio-DIO-Classificador-nivel-Her-i`).
2. Execute:

```powershell
node desafio3.js
```

- Saída:
  -`O saiyajin🐵 Goku, herói de 45 anos, atacou de forma desconhecida.`
  